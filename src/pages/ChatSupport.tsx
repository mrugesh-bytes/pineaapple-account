import React from 'react';
import Support from '../components/support/Support';

const ChatSupport = () => {
    return (
        <div>
            <Support />
        </div>
    );
};

export default ChatSupport;
